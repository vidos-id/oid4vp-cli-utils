## Highlights

- **Embed CLI version at build time**: CLI binaries now report their version via `--version` flag, built directly from package.json at bundle time
- **Include release notes in release**: Release workflow now automatically attaches generated release notes to GitHub Releases

## Other Changes

- Fix agents folder name
