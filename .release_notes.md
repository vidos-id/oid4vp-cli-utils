## Highlights

- **Publish scoped packages to GitHub Packages**: Libraries and CLIs renamed under `@vidos-id` scope with proper installation documentation for consumers
- **Fix release workflow**: Added `--registry` flag to `bun publish` to correctly target GitHub Packages

## Other Changes

- Update README files with installation instructions and CLI development usage
- Clarify usage of remote example inputs and their availability in GitHub Releases
- Add repository metadata to package.json files
- Create make-release skill for standardized releases
- Minor environment name updates
